numbers = '0123456789' - NUMBERS_TO_EXCLUDE = '0123456789' // список стринговых значений, которые нужно заменить в тексте.

не было константы - EAN_CODE_COL = 'ean_code'     // название столбца, который нужно находить в DataFrame'е.

не было константы - BRAND_NAME_COL = 'brand_name' // название столбца, который нужно находить в DataFrame'е.

operations = {
        1: 'addition',
        2: 'deletion',
        3: 'index',
        4: 'undo',
        5: 'redo'
    }
OPERATIONS = {
        1: 'addition',
        2: 'deletion',
        3: 'index',
        4: 'undo',
        5: 'redo'
    } 
// список реализуемых операций, не изменяется в ходе программы, заменить на константу

panel = {
        1: {
            2: close, 6: close, 9: close,
            5: diagonal, 8: diagonal,
        },
        2: {
            1: close, 5: close, 8: close, 3: close,
            6: diagonal, 9: diagonal, 4: diagonal, 7: diagonal,
            ...
// hard-coded цифровая панель, в словаре хранятся расстояния между цифрами, в ходе программы не изменяется, заменить на константу
DIGITAL_PANEL = {
        1: {
            2: close, 6: close, 9: close,
            5: diagonal, 8: diagonal,
        },
        2: {
            1: close, 5: close, 8: close, 3: close,
            6: diagonal, 9: diagonal, 4: diagonal, 7: diagonal,
            ...


printer_map = {
        ' ': 0,   '!': 9,  '"': 6,  '#': 24, '$': 29, '%': 22,
        ...
}
// hard-coded количество используемых принтером черних на символ, заменить на константу
PRINTER_MAP = {
        ' ': 0,   '!': 9,  '"': 6,  '#': 24, '$': 29, '%': 22,
        ...
}

boot_it = 1000 - BOOTSTRAP_SAMPLING = 1000 // количество бутстрап выборок из массива данных, обозначить как константу

не было константы - DB_PORT = 8443  // устанавливаем номер порта в базе данных как константу (раньше было просто число в коде)

не было константы - DB_NAME = 'db1'      // устанавливаем название базы данных как константу (раньше было просто число в коде)

не было константы - DB_USER = 'user'     // устанавливаем имя юзера в базе данных как константу (раньше было просто число в коде)

не было константы - DB_PASSWORD = 'xxxx' // устанавливаем номер порта в базе данных как константу (раньше было просто число в коде)

url = 'https://...' - URL_ADDRESS = 'https://...'   // устанавливаем URL адрес один раз в программе
